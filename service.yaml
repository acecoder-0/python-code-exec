apiVersion: v1
kind: Service
metadata:
  name: python-code-exec-service
spec:
  type: NodePort
  selector:
    app: python-code-exec
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007
